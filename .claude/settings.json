{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(zbb --slot * gate*)",
      "Bash(zbb gate --check)",
      "Bash(zbb --slot * env get *)",
      "Bash(zbb --slot * publishOrg)",
      "Bash(cd * && zbb --slot * gate*)",
      "Bash(cd * && zbb --slot * publishOrg)"
    ]
  }
}
