name: Publish
on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  id-token: write
  packages: write
  pull-requests: write

jobs:
  publish:
    uses: zerobias-org/devops/.github/workflows/content-release-reusable.yml@feat/nx-workflow-cleanup
    secrets: inherit
    with:
      skip-publish-announcement: 'false'
      test-timeout: 900
